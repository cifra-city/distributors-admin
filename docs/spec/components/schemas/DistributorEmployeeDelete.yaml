type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ distributor_employee_delete ]
      attributes:
        type: object
        required:
          - user_id
        properties:
          user_id:
            type: string
            description: "User ID"