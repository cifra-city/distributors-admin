type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ distributor_employee_update ]
      attributes:
        type: object
        required:
          - role
        properties:
          role:
            type: string
            description: "User role"