type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ distributor_update ]
      attributes:
        type: object
        required:
          - name
        properties:
          name:
            type: string
            description: "Distributor name"
